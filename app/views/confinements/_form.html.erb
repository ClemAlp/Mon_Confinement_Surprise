<div class="container">
<%= simple_form_for @confinement do |f| %>

  <div class="form-inputs">
    <%= f.input :name %>

  <%= f.input :description %>

  <%= f.input :photo, as: :file, label: 'Insérer une photo:', class: "btn-primary"%>
  <%= f.button :submit, class: "btn btn-primary btn-lg btn-block"  %>
<% end %>
</div>
